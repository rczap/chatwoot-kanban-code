datasource db {
  provider = "sqlite"
  url      = "file:/app/database.sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Board {
  id    String @id @default(uuid())
  name  String
}
